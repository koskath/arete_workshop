# Slide 17 of Lecture 13 contains information about the CNN Components.

In CNN modeling, the final stage is typically a fully connected (dense) layer equipped with activations such as ReLU or softmax to produce class probabilities, and dropout layers are often inserted to reduce overfitting by randomly zeroing activations during training.
