# Slide 19 of Lecture 2 contains information about the Ordinal encoding.

Ordinal encoding is similar to label encoding but is designed to maintain the inherent order of the values; because the penguin dataset lacks an ordinal text variable, the example instead demonstrates the concept using `body_mass_g` together with `sklearn.preprocessing.OrdinalEncoder()`.
