# Slide 30 of Lecture 5 contains information about the When to Consider Decision Trees?.

Decision trees are especially suitable when instances are naturally represented as attribute–value pairs, the target function has discrete outputs such as Boolean or multi-class labels, disjunctive descriptions are needed, and the training data may contain errors or missing values, which trees can handle robustly.
