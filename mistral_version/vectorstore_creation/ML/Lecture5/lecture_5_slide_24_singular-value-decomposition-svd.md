# Slide 24 of Lecture 5 contains information about the Singular Value Decomposition (SVD).

• SVD can also be represened as:
• Let A be a n×d matrix with singular vectors v , v , . . . , v and corresponding singular
1 2 r
values σ , σ , . . . , σ .
1 2 r
• Then u = (1/σ)*Av , for i = 1, 2, . . . , r, are left singular vectors. Where Rank(A) = r.
i i i
• A can be decomposed into a sum of rank one matrices as:
• A = UΣVT
OR
U∈ Rm×r has orthonormal columns; Σ = diag(σ , . . . , σ ), where σ ≥ · · · ≥ σ >0; V∈Rn×r has
1 r 1 r
orthonormal columns.
