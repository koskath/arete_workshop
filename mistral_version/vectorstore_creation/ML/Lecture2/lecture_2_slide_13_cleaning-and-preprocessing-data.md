# Slide 13 of Lecture 2 contains information about the Cleaning and Preprocessing Data.

Some ways to clean and preprocess data include converting text fields into numeric form, removing rows with missing or faulty entries, fixing rows with bad datapoints, dropping unnecessary variables, scaling or normalizing features, creating new variables, and renaming fields to remove capitalization and spaces.
